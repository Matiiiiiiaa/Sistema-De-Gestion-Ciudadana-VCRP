<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Registrar Multa | Carabineros RP</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="dashboard-body">

<header class="nav-header">
    <div class="nav-left">
        <img src="/img/logo.png" class="nav-logo">
        <span class="nav-role">Funcionario: <b><%= funcionario.usuario %></b></span>
    </div>
    <a href="/dashboard_funcionario" class="logout-btn">Volver</a>
</header>

<div class="func-form">

    <h2>Registrar Multa</h2>
    <p class="form-subtitle">Complete los datos solicitados</p>

    <% if (success) { %>
        <div class="success-box"><%= success %></div>
        <a href="/dashboard_funcionario" class="back-link">⬅ Volver</a>
    <% } else { %>

        <% if (error) { %>
            <div class="error-box"><%= error %></div>
        <% } %>

        <form method="POST">

            <label>Usuario (Discord)</label>
            <input type="text" name="usuario" class="input" placeholder="Ej: PepitoRP" required>

            <label>Nombre</label>
            <input type="text" name="nombre" class="input" required>

            <label>Apellido</label>
            <input type="text" name="apellido" class="input" required>

            <label>RUT</label>
            <input type="text" name="rut" class="input" required>

            <label>Artículo</label>
            <input type="text" name="articulo" class="input" required>

            <label>Monto</label>
            <input type="number" name="monto" class="input" required>

            <label>Válido</label>
            <select name="valido" class="input" required>
                <option value="Sí">Sí</option>
                <option value="No">No</option>
            </select>

            <button class="btn-funcionario" type="submit">Registrar Multa</button>

        </form>

    <% } %>

</div>

</body>
</html>
